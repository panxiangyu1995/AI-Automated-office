root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/server.exe ./cmd/server"
bin = "tmp/server.exe"
include_ext = ["go", "yaml"]
exclude_dir = ["tmp", "vendor"]

[run]
cmd = "./tmp/server.exe"
