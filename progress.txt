# Progress: Agent核心模块架构优化

## 2026-04-19 - Task 79: Agent核心模块架构优化

### 铁律合规检查：
- PRD 合规：通过（架构优化不改变功能需求）
- 架构合规：符合分层微内核架构、DIP、单一职责原则
- 优化原则：通过（KISS/DRY/SOLID/DIP/YAGNI）

### 架构优化内容：
- 优化范围：src-tauri/src/agent/ + src/features/agent/
- 优化前问题：6大架构问题（编译耦合、并发原语不统一、Provider模式僵硬、前端接口膨胀、大文件集中、路由逻辑混杂）
- 生成的 task：task.json Task 79
- OpenSpec 变更：openspec/changes/iterate-agent-architecture/

### 本次完成的具体修改：

#### Task 2: 并发原语统一
- `src-tauri/src/agent/tools/registry.rs`
  - `Mutex<HashMap>` → `Arc<RwLock<HashMap>>`
  - 分离 `read_tools()` 和 `write_tools()` 私有方法
  - 保留 PoisonError → RegistryError 转换逻辑

#### Task 3: Provider Strategy 模式
- `src-tauri/src/agent/dual_agent_provider.rs`（新增）
  - 实现 Plan/Act 双模式切换的 Strategy 模式
  - `DualAgentProvider` 结构体支持 `get_mode()` / `set_mode()`
  - 已在 `agent/mod.rs` 导出
- `src-tauri/src/agent/mod.rs`（修改）
  - 新增 `pub mod dual_agent_provider`

#### Task 4: 前端按域拆分
- 新增 9 个功能域子 index 文件：
  - `components/chat/index.ts` - 对话组件
  - `components/collaboration/index.ts` - Agent 协作组件
  - `components/monitoring/index.ts` - 可观测性组件
  - `components/pilot/index.ts` - Pilot 集成组件
  - `components/checkpoint/index.ts` - 检查点组件
  - `components/error/index.ts` - 错误处理组件
  - `components/search/index.ts` - 搜索组件
  - `components/workcard/index.ts` - 工作卡片组件
  - `components/core/index.ts` - 核心组件
- 新增 `components/index.ts` 聚合导出
- 新增 `hooks/domains/` 按域聚合 hooks（useChat.ts, useCheckpoint.ts, useGit.ts, useCompression.ts）
- 修改 `features/agent/index.ts` 精简导出，保留兼容层

#### Task 5 部分: enterprise.rs 大文件拆分
- 创建 `tools/enterprise/` 子目录（6 个模块文件）
  - `mod.rs` - 目录入口，聚合注册
  - `resource.rs` (131行) - ResourceQueryExecutor + ResourceUploadExecutor
  - `knowledge.rs` (125行) - KnowledgeQueryExecutor + KnowledgeSubmitDraftExecutor
  - `messaging.rs` (142行) - MessageQueryExecutor + MessageSendExecutor
  - `delegation.rs` (98行) - AgentDelegateExecutor
  - `workspace.rs` (96行) - WorkspaceStageChangeExecutor
  - `database.rs` (111行) - DbQueryExecutor
- 删除原有 `tools/enterprise.rs`（892行 → 拆分为最大142行）

### 功能不变性验证：
- [x] `cargo check` 编译成功
- [x] TypeScript agent 模块无编译错误
- [ ] `cargo test --lib` 单元测试（预存错误，非本次修改引入）
- [ ] 工具注册流程功能回归测试（待完整实施后验证）
- [ ] Plan/Act 模式切换功能回归测试（待集成到 RuntimeConfig 后验证）

### 待完成的工作：
1. Task 1: pipeline.rs 引入 `ToolModule` trait 动态自注册
2. Task 6: document.rs 和 filesystem.rs 拆分
3. DualAgentProvider 集成到 RuntimeConfig 初始化流程
4. 完整功能回归测试

### 注意事项：
- enterprise.rs 拆分后，pipeline.rs 中的 `use super::enterprise` 导入路径仍然有效（因为 `mod.rs` 重新导出）
- 前端 TypeScript 编译错误仅存在于 message 模块（非本次修改）
- Rust 测试编译错误预存于 error_recovery.rs（与本次修改无关）
