# ai-office - 系统不变量

> 不可违反的系统边界。违反其中任何一条 = CRITICAL Bug。
> 每条不变量应注明：能否自动化？当前状态（已有测试 / 无测试 / 人工检查）

## 安全边界

- INV-1: 所有外部通信必须使用 TLS 1.3 — 状态：无测试
- INV-2: 敏感数据必须使用 AES-256 加密存储 — 状态：无测试
- INV-3: 用户认证 token 必须有过期时间和刷新机制 — 状态：无测试

## 数据隔离

- INV-4: 多租户数据必须数据库级隔离 — 状态：无测试
- INV-5: 本地数据优先，云端增量同步 — 状态：无测试

## 接口契约

- INV-6: 前后端 API 字段名必须与 api-contracts.md 一致 — 状态：人工检查
- INV-7: 工具命名必须遵循 {plugin}_{entity}_{action} 格式 — 状态：C1 已验证（6部门30工具全部合规）

## 铁律合规

- INV-8: 所有功能实现必须有 PRD 需求编号来源 — 状态：人工检查
- INV-9: 所有 UI 必须使用 Shadcn/ui 组件 + Lucide React 图标 — 状态：人工检查
- INV-10: 颜色系统必须遵循 UX 设计规范 (#1E3A5F 主色) — 状态：C1 部分合规（21文件/250处使用 var(--ao-*)，129文件/1411处仍硬编码）

---

当识别到反复出现的 Bug 模式（通过 reviewer 或 Known Pitfalls），考虑将其添加为正式不变量。目标：reviewer 成为**第二道**防线；自动化测试是**第一道**。
