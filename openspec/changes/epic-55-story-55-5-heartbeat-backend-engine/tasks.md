# Tasks: 心跳机制后端执行引擎

## 任务概览

| ID | 任务 | 优先级 | 状态 |
|----|------|--------|------|
| T1 | 创建心跳模块骨架 | Critical | Pending |
| T2 | 实现心跳配置结构 | Critical | Pending |
| T3 | 实现预检查器 | Critical | Pending |
| T4 | 实现HEARTBEAT.md解析器 | High | Pending |
| T5 | 实现心跳执行器 | High | Pending |
| T6 | 实现心跳调度器 | High | Pending |
| T7 | 实现事件发射器 | Medium | Pending |
| T8 | 实现投递机制 | Medium | Pending |
| T9 | 注册Tauri命令 | Critical | Pending |
| T10 | 前后端集成测试 | High | Pending |

---

## T1: 创建心跳模块骨架

**优先级**: Critical  
**预估复杂度**: Low  
**依赖**: 无

### 描述
创建 `src-tauri/src/agent/heartbeat.rs` 模块骨架，定义模块结构和导出。

### 验收标准
- [ ] 创建 `heartbeat.rs` 文件
- [ ] 创建 `heartbeat/` 子目录
- [ ] 创建子模块文件: `mod.rs`, `config.rs`, `scheduler.rs`, `preflight.rs`, `executor.rs`, `parser.rs`, `events.rs`, `delivery.rs`
- [ ] 更新 `src-tauri/src/agent/mod.rs` 导出心跳模块
- [ ] 编译通过

### 实现步骤
1. 创建 `heartbeat.rs` 主模块文件
2. 创建 `heartbeat/` 子目录
3. 创建各子模块文件
4. 更新 `mod.rs` 导出

---

## T2: 实现心跳配置结构

**优先级**: Critical  
**预估复杂度**: Medium  
**依赖**: T1

### 描述
实现心跳配置数据结构，支持所有FR1127-FR1134需求。

### 验收标准
- [ ] 实现 `HeartbeatConfig` 结构体
- [ ] 实现 `ActiveHours` 结构体
- [ ] 实现 `DeliveryTarget` 结构体
- [ ] 支持序列化/反序列化
- [ ] 提供默认配置（30分钟间隔）
- [ ] 单元测试覆盖

### 实现步骤
1. 在 `config.rs` 中定义配置结构
2. 实现 `Default` trait
3. 添加验证逻辑
4. 编写单元测试

---

## T3: 实现预检查器

**优先级**: Critical  
**预估复杂度**: Medium  
**依赖**: T1, T2

### 描述
实现心跳执行前的预检查逻辑，确保心跳只在合适条件下执行。

### 验收标准
- [ ] 实现用户状态检查
- [ ] 实现活动时段检查 (FR1130)
- [ ] 实现资源可用性检查
- [ ] 实现上下文预算检查
- [ ] 实现请求队列检查
- [ ] 实现 `PreflightResult` 结构
- [ ] 实现 `SkipReason` 枚举
- [ ] 单元测试覆盖

### 实现步骤
1. 在 `preflight.rs` 中定义预检查结构
2. 实现各项检查逻辑
3. 实现检查结果聚合
4. 编写单元测试

---

## T4: 实现HEARTBEAT.md解析器

**优先级**: High  
**预估复杂度**: Medium  
**依赖**: T1

### 描述
实现HEARTBEAT.md文件解析，支持检查清单提取和跳过指令识别。

### 验收标准
- [ ] 支持读取工作区HEARTBEAT.md (FR1128)
- [ ] 支持解析Markdown检查项
- [ ] 支持识别跳过指令 (FR1134)
- [ ] 实现 `HeartbeatMdContent` 结构
- [ ] 实现 `CheckItem` 结构
- [ ] 实现 `CheckItemStatus` 枚举
- [ ] 单元测试覆盖

### 实现步骤
1. 在 `parser.rs` 中定义解析结构
2. 实现文件读取逻辑
3. 实现Markdown解析
4. 实现检查项提取
5. 编写单元测试

---

## T5: 实现心跳执行器

**优先级**: High  
**预估复杂度**: High  
**依赖**: T2, T3, T4

### 描述
实现心跳执行核心逻辑，支持检查清单执行和结果处理。

### 验收标准
- [ ] 实现心跳执行主流程
- [ ] 支持HEARTBEAT_OK静默处理 (FR1129)
- [ ] 支持隔离会话模式 (FR1131)
- [ ] 支持轻量上下文模式 (FR1131)
- [ ] 实现 `HeartbeatRunResult` 结构
- [ ] 实现 `HeartbeatStatus` 枚举
- [ ] 集成测试覆盖

### 实现步骤
1. 在 `executor.rs` 中定义执行器
2. 实现执行主流程
3. 实现结果处理
4. 实现会话模式切换
5. 编写集成测试

---

## T6: 实现心跳调度器

**优先级**: High  
**预估复杂度**: Medium  
**依赖**: T2, T5

### 描述
实现心跳定时调度器，支持可配置间隔和活动时段。

### 验收标准
- [ ] 支持可配置间隔（默认30分钟）(FR1127)
- [ ] 支持活动时段配置 (FR1130)
- [ ] 支持时区设置 (FR1130)
- [ ] 支持立即唤醒模式 (FR1133)
- [ ] 支持"下次心跳"执行模式 (FR1133)
- [ ] 支持启动/停止控制
- [ ] 单元测试覆盖

### 实现步骤
1. 在 `scheduler.rs` 中定义调度器
2. 实现定时调度逻辑
3. 实现活动时段检查
4. 实现唤醒模式
5. 编写单元测试

---

## T7: 实现事件发射器

**优先级**: Medium  
**预估复杂度**: Low  
**依赖**: T1

### 描述
实现心跳事件发射，支持状态变更通知和审计日志。

### 验收标准
- [ ] 实现 `HeartbeatEvent` 枚举
- [ ] 支持心跳开始事件
- [ ] 支持心跳跳过事件
- [ ] 支持心跳完成事件
- [ ] 支持心跳失败事件
- [ ] 支持检查项状态变更事件
- [ ] 集成现有事件系统
- [ ] 单元测试覆盖

### 实现步骤
1. 在 `events.rs` 中定义事件结构
2. 实现事件发射逻辑
3. 集成Tauri事件系统
4. 编写单元测试

---

## T8: 实现投递机制

**优先级**: Medium  
**预估复杂度**: Medium  
**依赖**: T2, T5

### 描述
实现心跳通知投递机制，支持显式投递目标和通知策略。

### 验收标准
- [ ] 支持显式投递目标配置 (FR1132)
- [ ] 支持通知策略配置 (FR1132)
- [ ] 支持多渠道投递
- [ ] 实现 `HeartbeatNotification` 结构
- [ ] 实现 `NotificationLevel` 枚举
- [ ] 单元测试覆盖

### 实现步骤
1. 在 `delivery.rs` 中定义投递结构
2. 实现投递目标解析
3. 实现通知发送逻辑
4. 编写单元测试

---

## T9: 注册Tauri命令

**优先级**: Critical  
**预估复杂度**: Low  
**依赖**: T5, T6

### 描述
注册心跳相关的Tauri命令，供前端调用。

### 验收标准
- [ ] 实现 `start_heartbeat` 命令
- [ ] 实现 `stop_heartbeat` 命令
- [ ] 实现 `trigger_heartbeat_now` 命令
- [ ] 实现 `get_heartbeat_status` 命令
- [ ] 实现 `update_heartbeat_config` 命令
- [ ] 在 `lib.rs` 中注册命令
- [ ] 编译通过

### 实现步骤
1. 在 `heartbeat.rs` 中实现命令函数
2. 添加 `#[tauri::command]` 宏
3. 在 `lib.rs` 的 `invoke_handler` 中注册
4. 测试命令调用

---

## T10: 前后端集成测试

**优先级**: High  
**预估复杂度**: High  
**依赖**: T9

### 描述
完成前后端集成测试，确保心跳功能端到端可用。

### 验收标准
- [ ] 前端能启动/停止心跳
- [ ] 前端能接收心跳事件
- [ ] 前端能显示心跳状态
- [ ] 前端能触发立即心跳
- [ ] 前端能更新心跳配置
- [ ] HEARTBEAT.md正确解析
- [ ] 静默处理正确
- [ ] E2E测试通过

### 实现步骤
1. 编写前端集成测试
2. 编写E2E测试
3. 验证所有验收标准
4. 修复发现的问题

---

## 任务依赖图

```
T1 (模块骨架)
├── T2 (配置结构) ──┬── T3 (预检查器) ──┐
│                   │                    │
│                   └── T5 (执行器) ──────┼── T9 (Tauri命令) ── T10 (集成测试)
│                                        │
├── T4 (HEARTBEAT.md解析器) ─────────────┘
│
├── T6 (调度器) ──────────────────────────┘
│
└── T7 (事件发射器)
    └── T8 (投递机制)
```

## 里程碑

| 里程碑 | 任务 | 预期产出 |
|--------|------|----------|
| M1: 核心骨架 | T1, T2 | 模块结构完成，配置可解析 |
| M2: 预检查与解析 | T3, T4 | 预检查逻辑完成，HEARTBEAT.md可解析 |
| M3: 执行与调度 | T5, T6 | 心跳可执行，可调度 |
| M4: 事件与投递 | T7, T8 | 事件可发射，通知可投递 |
| M5: 集成完成 | T9, T10 | 前后端集成完成，E2E可用 |
