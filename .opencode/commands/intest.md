1.尝试构建前后端确保没有报错，2.其次使用终端命令的方式在数据库执行这个迁移数据库确保这个sql脚本能被正确执行3.然后运行后端，通过终端使用curl命令模拟进行本地测试数据库的模拟测试，所有api测试通过之后，4.再运行前端使用优先使用agent-browser skill（无法使用agent-browser skill时再使用playwright 或者chrome devtools mcp）检测前端UI组件的操作逻辑是否有问题，检查UI的渲染、前端数据的传递还有前后端一致性是否有问题，测试方式要模拟各种可能性的操作确保不错因为操作方式不同导致BUG。
测试数据你可以自己创建sql，终端执行写入到本地数据库中进行测试。直至上述所有的测试点都测试无误、前端浏览器E2E测试时无相关测试功能的报错和警告（跟开发不相关的报错和警告不必处理）之后再退出这次的测试。如果有中断的问题，你重新尝试2次，不超过3次，一旦超过3次立即停下进行深度反思。
